# Copyright 2026 Leonan Carvalho
# SPDX-License-Identifier: AGPL-3.0-only

name: Benchmark

on:
  push:
    tags:
      - 'v*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      mode:
        description: 'Benchmark mode'
        type: choice
        options: [theoretical, live]
        default: theoretical

permissions:
  contents: write

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Check out the branch the release targets (usually main) so the
          # auto-commit action can push RESULTS.md back instead of a detached HEAD.
          ref: ${{ github.event.release.target_commitish || github.ref || 'main' }}
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run benchmark
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ "${{ github.event.inputs.mode }}" = "theoretical" ]; then
            make benchmark VERSION=${{ github.ref_name }}
          else
            make benchmark-live VERSION=${{ github.ref_name }}
          fi

      - name: Commit updated RESULTS.md
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "bench: update RESULTS.md for ${{ github.ref_name }} [skip ci]"
          file_pattern: benchmark/RESULTS.md
